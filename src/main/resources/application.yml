spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/rabiya_supply
        username: postgres
        password: JonaMia

    jpa:
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect

server:
    port: 8080
